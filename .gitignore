# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — use .env.example as reference
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
server.log

# skills / internal
/skills/
test
prompt

# lockfiles (use npm install fresh on deploy)
package-lock.json

# prisma local db
prisma/db/

# soc-engine (local Bun service — not deployed to Vercel)
mini-services/soc-engine/node_modules/
mini-services/soc-engine/bun.lock

# ml artifacts — large files, stored in repo intentionally; exclude local scratch
ml/artifacts/model_registry.json
ml/scripts/
ml/training/